server:
  port: 8080

spring:
  application:
    name: gateway
  cloud:
    gateway:
      routes:
        # ========== 用户服务 ==========
        - id: user-service
          uri: http://user-service:8081
          predicates:
            - Path=/api/users/**
          filters:
            - StripPrefix=1

        # ========== 商品服务 ==========
        - id: product-service
          uri: http://product-service:8083
          predicates:
            - Path=/api/products/**
          filters:
            - StripPrefix=1

        # ========== 订单服务 ==========
        - id: order-service
          uri: http://order-service:8085
          predicates:
            - Path=/api/orders/**
          filters:
            - StripPrefix=1

        # ========== 秒杀服务 ==========
        - id: seckill-service
          uri: http://seckill-service:8086
          predicates:
            - Path=/api/seckill/**
          filters:
            - StripPrefix=1

        # ========== 搜索服务 ==========
        - id: search-service
          uri: http://product-service:8083
          predicates:
            - Path=/api/search/**
          filters:
            - StripPrefix=1

      # ========== 全局跨域配置 ==========
      globalcors:
        cors-configurations:
          '[/**]':
            allowedOrigins: "*"
            allowedMethods:
              - GET
              - POST
              - PUT
              - DELETE
              - OPTIONS
            allowedHeaders: "*"
            allowCredentials: false
            maxAge: 3600

      # ========== 默认过滤器 ==========
      default-filters:
        - DedupeResponseHeader=Access-Control-Allow-Origin Access-Control-Allow-Credentials, RETAIN_FIRST

management:
  endpoints:
    web:
      exposure:
        include: health,info,gateway
  endpoint:
    health:
      show-details: always
    gateway:
      enabled: true
